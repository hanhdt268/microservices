server:
  port: 8085


spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hanhdt268/microservice-config
          clone-on-start: true
